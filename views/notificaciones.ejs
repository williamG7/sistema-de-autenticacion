<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>notificaciones</title>
    <link rel="stylesheet" href="style.css">
</head>

<body>
    <header>
        <h1>Gestion de notificaciones por <%= user.nombre %>
        </h1>
        <h2><%- htmlMessage %></h2>
    </header>
    <main>
        <section>
            <h2>Lista de notificaciones</h2>
            <ul class="lista">
                <% data.notificaciones.forEach(notificacion=> { %>
                    <li class="item">
                        <strong>
                            <%= notificacion.nombre %>
                        </strong>
                        <span>usuarioId: <%= notificacion.usuarioId %></span>
                        <span>mensaje: <%= notificacion.mensaje %></span>
                        <span>mensaje leido: <%= notificacion.leida %></span>
                        <span><a href="/notificaciones/<%= notificacion.id %>" class="btn-formulario">detalle
                                de la notificacion</a></span>
                        <span><a href="/formularioNotificaciones.html" class="btn-formulario">Editar
                                notificacion</a></span>
                    </li>
                    <% }) %>
            </ul>
        </section>
    </main>
</body>

</html>